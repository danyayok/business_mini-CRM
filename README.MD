# CRM System - Multi-tenant Business CRM

## 📋 О проекте

Многопользовательская мини-CRM система с поддержкой нескольких организаций (multi-tenant architecture). Система предоставляет полный цикл управления клиентами, сделками и задачами с разграничением прав доступа.

## 🏗️ Архитектура

### Слои приложения:
- **API Layer** (`app/api/`) - FastAPI роутеры, валидация запросов
- **Services Layer** (`app/services/`) - бизнес-логика, правила валидации
- **Repositories Layer** (`app/repos/`) - работа с БД, CRUD операции  
- **Models Layer** (`app/models/`) - SQLAlchemy ORM модели
- **Schemas** (`app/schemas/`) - Pydantic схемы для валидации

### Технологический стек:
- **Python 3.9+** с type hints
- **FastAPI** - async web framework
- **SQLAlchemy 2.0 + Alembic** - ORM и миграции
- **PostgreSQL** - основная БД
- **Redis** - кэширование
- **JWT** - аутентификация
- **Docker + Docker-compose** - контейнеризация

## 🚀 Быстрый старт

### 1. Клонирование и настройка
```bash
git clone <repository>
cd business_crm
cp .env.example .env
# Отредактируйте .env под ваше окружение
```

### 2. Запуск через Docker
```bash
chmod +x deploy.sh
./deploy.sh
```

### 3. Ручной запуск
```bash
docker-compose up --build -d
```

### 4. Проверка работоспособности
```bash
curl http://localhost:8000/health
# Документация API: http://localhost:8000/docs
```

## 📊 Модель данных

### Основные сущности:
- **Organizations** - компании/организации
- **Users** - пользователи системы  
- **OrganizationMembers** - связь пользователей с организациями и роли
- **Contacts** - контакты клиентов
- **Deals** - сделки с этапами воронки
- **Tasks** - задачи по сделкам
- **Activities** - таймлайн активности

### Роли пользователей:
- **owner** - полный доступ ко всем функциям организации
- **admin** - администрирование, кроме удаления организации
- **manager** - управление сущностями, кроме настроек организации  
- **member** - просмотр всего, редактирование только своих данных

## 🔐 API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` - регистрация пользователя и организации
- `POST /api/v1/auth/login` - вход в систему

### Организации
- `GET /api/v1/organizations/me` - мои организации
- `GET /api/v1/organizations/{org_id}/members` - участники организации
- `POST /api/v1/organizations/{org_id}/members` - добавить участника
- `PATCH /api/v1/organizations/{org_id}/members/{user_id}` - изменить роль
- `DELETE /api/v1/organizations/{org_id}/members/{user_id}` - удалить участника

### Контакты
- `GET /api/v1/contacts` - список контактов (с фильтрацией)
- `POST /api/v1/contacts` - создать контакт
- `DELETE /api/v1/contacts/{contact_id}` - удалить контакт

### Сделки  
- `GET /api/v1/deals` - список сделок (расширенная фильтрация)
- `POST /api/v1/deals` - создать сделку
- `PATCH /api/v1/deals/{deal_id}` - обновить сделку

### Задачи
- `GET /api/v1/tasks` - список задач (фильтрация по deal_id, статусу)
- `POST /api/v1/tasks` - создать задачу

### Активности
- `GET /api/v1/deals/{deal_id}/activities` - активности сделки
- `POST /api/v1/deals/{deal_id}/activities` - добавить активность

### Аналитика
- `GET /api/v1/analytics/deals/summary` - сводка по сделкам
- `GET /api/v1/analytics/deals/funnel` - воронка продаж

## ⚙️ Бизнес-правила

### Валидации и ограничения:

1. **Multi-tenant изоляция**
   - Все запросы выполняются в контексте организации (X-Organization-Id)
   - Доступ только к данным своей организации

2. **Права доступа**
   - owner/admin: полный доступ
   - manager: управление сущностями
   - member: только свои контакты/сделки/задачи

3. **Валидация сделок**
   - Нельзя закрыть сделку как "won" с amount <= 0
   - Контроль переходов между стадиями
   - Запрет отката стадий для member/manager

4. **Валидация контактов** 
   - Нельзя удалить контакт с активными сделками
   - Проверка принадлежности организации

5. **Валидация задач**
   - Due date не может быть в прошлом
   - Привязка только к сделкам своей организации

6. **Автоматизация**
   - Автоматическое создание Activity при изменении статуса/стадии
   - Обновление updated_at при изменении сделки

## 🧪 Тестирование

### Запуск тестов:
```bash
python tests/run_tests.py
# или
pytest tests/ -v
```

### Структура тестов:
- `test_services.py` - unit-тесты бизнес-логики
- `test_business_rules.py` - тесты валидаций и правил
- `test_full_flow.py` - интеграционные тесты полного цикла

### Тестовые сценарии:
- Регистрация и аутентификация
- Управление участниками организаций
- Полный цикл: контакт → сделка → задачи
- Проверка бизнес-правил и валидаций
- Тесты прав доступа для разных ролей

## 🔧 Конфигурация

### Переменные окружения (.env):
```env
DATABASE_URL=postgresql://user:pass@db:5432/crm_db
SECRET_KEY=vstavte_secretny_kod_syuda
REDIS_URL=redis://redis:6379
ALGORITHM=HS256
CORS_ORIGINS=["http://localhost:3000"]
ENVIRONMENT=production
```

### Миграции БД:
```bash
# Автоматически при запуске контейнера
# Или вручную:
docker-compose exec web alembic upgrade head
```

## Аналитика и кэширование

### Сводка по сделкам:
- Количество сделок по статусам
- Суммы по статусам  
- Средний размер выигранных сделок
- Новые сделки за 30 дней

### Воронка продаж:
- Количество сделок по стадиям
- Конверсия между стадиями
- Кэширование на 60 секунд

## Мониторинг и диагностика

### Health checks:
- `GET /health` - статус приложения
- `GET /docs` - документация API
- Логи в реальном времени: `docker-compose logs -f web`

### Возможные проблемы:
1. **Нет подключения к БД** - проверить переменные DATABASE_URL
2. **Ошибки миграций** - выполнить `alembic upgrade head`
3. **Проблемы с Redis** - проверить REDIS_URL и доступность Redis

## 📄 Лицензия

MIT License - полный текст в файле LICENSE